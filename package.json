{
  "name": "art-philosophy-secondbrain",
  "version": "1.0.0",
  "description": "A personal second brain for art and philosophy, published as a web resource",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next build",
    "deploy": "npm run export && git add out -f && git commit -m 'Deploy to GitHub Pages' && git push",
    "suggest-articles": "tsx scripts/suggest-articles.ts",
    "generate-article": "tsx scripts/generate-article.ts",
    "extract-claims": "tsx scripts/extract-claims.ts",
    "apply-fact-check": "tsx scripts/apply-fact-check-prompt.ts"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^11.0.0",
    "next": "^14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "rss": "^1.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0"
  }
}
